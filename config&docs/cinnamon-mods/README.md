# Historical Cinnamon keybinding note

`keybindings.md` preserves the local August 20, 2026 note unchanged.
It records the Ctrl+Shift+W setup from that date, not the current recommended
configuration. The existing `../cinnamon-settings.tsv` specifies Alt+F4 and
Super+Q instead. Recovery does not apply either configuration or change the
running desktop. The original `~/cinnamon-mods` path points to this folder.
